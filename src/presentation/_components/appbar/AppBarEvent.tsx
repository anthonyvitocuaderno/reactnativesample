export default class AppBarEvent {

    static Focused = (class extends AppBarEvent{})
    static LogoutPressed = (class extends AppBarEvent{})
    static BackPressed = (class extends AppBarEvent{})
}
